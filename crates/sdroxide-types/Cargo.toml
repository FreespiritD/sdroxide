[package]
name = "sdroxide-types"
version.workspace = true
edition.workspace = true

[dependencies]
serde.workspace = true

[dev-dependencies]
# Round-trip tests for the types `sdroxide-config` persists as JSON.
serde_json = "1"
